// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: proto/engine/v1/quote.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "engine.v1";

export interface QuoteRequest {
  playerId: string;
  /** e.g., "EPL" */
  market: string;
  /** correlation id */
  reqId: string;
}

export interface QuoteResponse {
  playerId: string;
  bid: number;
  ask: number;
  mid: number;
  tsMs: number;
  /** mirrors request */
  reqId: string;
}

function createBaseQuoteRequest(): QuoteRequest {
  return { playerId: "", market: "", reqId: "" };
}

export const QuoteRequest: MessageFns<QuoteRequest> = {
  encode(message: QuoteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.market !== "") {
      writer.uint32(18).string(message.market);
    }
    if (message.reqId !== "") {
      writer.uint32(26).string(message.reqId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuoteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.market = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reqId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuoteRequest {
    return {
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      market: isSet(object.market) ? globalThis.String(object.market) : "",
      reqId: isSet(object.reqId) ? globalThis.String(object.reqId) : "",
    };
  },

  toJSON(message: QuoteRequest): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.market !== "") {
      obj.market = message.market;
    }
    if (message.reqId !== "") {
      obj.reqId = message.reqId;
    }
    return obj;
  },

  create(base?: DeepPartial<QuoteRequest>): QuoteRequest {
    return QuoteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuoteRequest>): QuoteRequest {
    const message = createBaseQuoteRequest();
    message.playerId = object.playerId ?? "";
    message.market = object.market ?? "";
    message.reqId = object.reqId ?? "";
    return message;
  },
};

function createBaseQuoteResponse(): QuoteResponse {
  return { playerId: "", bid: 0, ask: 0, mid: 0, tsMs: 0, reqId: "" };
}

export const QuoteResponse: MessageFns<QuoteResponse> = {
  encode(message: QuoteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.bid !== 0) {
      writer.uint32(17).double(message.bid);
    }
    if (message.ask !== 0) {
      writer.uint32(25).double(message.ask);
    }
    if (message.mid !== 0) {
      writer.uint32(33).double(message.mid);
    }
    if (message.tsMs !== 0) {
      writer.uint32(40).int64(message.tsMs);
    }
    if (message.reqId !== "") {
      writer.uint32(50).string(message.reqId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuoteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuoteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.bid = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.ask = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.mid = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tsMs = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.reqId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuoteResponse {
    return {
      playerId: isSet(object.playerId) ? globalThis.String(object.playerId) : "",
      bid: isSet(object.bid) ? globalThis.Number(object.bid) : 0,
      ask: isSet(object.ask) ? globalThis.Number(object.ask) : 0,
      mid: isSet(object.mid) ? globalThis.Number(object.mid) : 0,
      tsMs: isSet(object.tsMs) ? globalThis.Number(object.tsMs) : 0,
      reqId: isSet(object.reqId) ? globalThis.String(object.reqId) : "",
    };
  },

  toJSON(message: QuoteResponse): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.bid !== 0) {
      obj.bid = message.bid;
    }
    if (message.ask !== 0) {
      obj.ask = message.ask;
    }
    if (message.mid !== 0) {
      obj.mid = message.mid;
    }
    if (message.tsMs !== 0) {
      obj.tsMs = Math.round(message.tsMs);
    }
    if (message.reqId !== "") {
      obj.reqId = message.reqId;
    }
    return obj;
  },

  create(base?: DeepPartial<QuoteResponse>): QuoteResponse {
    return QuoteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuoteResponse>): QuoteResponse {
    const message = createBaseQuoteResponse();
    message.playerId = object.playerId ?? "";
    message.bid = object.bid ?? 0;
    message.ask = object.ask ?? 0;
    message.mid = object.mid ?? 0;
    message.tsMs = object.tsMs ?? 0;
    message.reqId = object.reqId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
